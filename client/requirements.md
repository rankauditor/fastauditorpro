## Packages
framer-motion | Complex animations for layout transitions and audit reveals
react-dropzone | Drag and drop file upload handling
date-fns | Formatting dates and durations

## Notes
- Upload endpoint expects multipart/form-data with 'file' field
- Poll /api/calls/:id when status is not 'completed' or 'failed'
- Tailwind config should extend fontFamily with 'inter' and 'space-grotesk'
